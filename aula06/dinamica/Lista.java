public class Lista {
    private No inicio;

    public Lista() {
        inicio = null;
    }

    // verificar se a lista está vazia
    public boolean estaVazia() {
        return (inicio == null);
    }

    // inserir na lista
    // remover da lista
    // exibir a lista
    // tamanho da lista
    // procurar na lista
}
